#include "holberton.h"
/**
 *add - functiont to add 2 numbers
 *
 * @a: one of the numbers of the sum
 * @b: another number of the sum
 *
 * Return: returns 0 (always success)
 */
int add(int a, int b)
{
	return (a + b);
}
